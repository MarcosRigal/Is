#include "monitor.h"

bool notifyIncidentOnRoute(int parkID, int routeID, string incidence){
    /* Iterar la lista de parques hasta encontrar el parque */
    /* Iterar la lista de rutas (dentro del parque seleccionado arriba) hasta encontrar la ruta */
    /* Hacer un push.back() del string a la lista de incidencias de la ruta */
    /* NO PUEDO ITERAR UNA LISTA QUE ESTA EN EL MAIN!!!!! */

}
